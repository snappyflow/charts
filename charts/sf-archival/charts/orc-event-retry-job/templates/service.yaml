apiVersion: v1
kind: Service
metadata:
  name: {{ include "orc-event-retry-job.fullname" . }}
  labels:
    {{- include "orc-event-retry-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orc-event-retry-job.selectorLabels" . | nindent 4 }}
